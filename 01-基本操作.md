# 基本操作

* 查看 `Git` 版本

```bash
 git version
```

* 基本配置

```bash
git config --global user.name "lwifew"
git config --global user.email "gfiszhang@gmail.com"
```

* 添加文件到暂存区

```bash
git add file_name
```
